mutation SaveCipher(
  $id: String
  $name: String!
  $correctAnswer: String!
  $published: Boolean!
  $fileLink: String
) {
  saveCipher(
    id: $id
    name: $name
    correctAnswer: $correctAnswer
    published: $published
    fileLink: $fileLink
  ) {
    ...Cipher
  }
}
